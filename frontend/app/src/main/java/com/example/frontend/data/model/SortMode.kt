package com.example.frontend.data.model

enum class SortMode {
    CUSTOM,    // Initial order
    DATE,      // Sort by date
    QUANTITY,  // Sort by quantity
    CHECKED    // Put the picked items at the end
}
